# Port the Node server listens on
PORT=3100

# Directory where books.json and cached covers live (created on first run)
DATA_DIR=./data

# Optional single-password HTTP Basic Auth.
# Set ENABLE_AUTH=true and provide credentials to protect a public deployment.
ENABLE_AUTH=true
AUTH_USER=admin
AUTH_PASS=change-me
