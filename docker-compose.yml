services:
  app:
    build: .
    restart: unless-stopped
    environment:
      - PORT=3100
      - DATA_DIR=/app/data
      - ENABLE_AUTH=${ENABLE_AUTH:-false}
      - AUTH_USER=${AUTH_USER:-admin}
      - AUTH_PASS=${AUTH_PASS}
    volumes:
      - ./data:/app/data
    expose:
      - "3100"
